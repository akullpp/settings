{
  "name": "leaf",
  "version": "0.0.1",
  "private": "true",
  "main": "dist/index.js",
  "scripts": {
    "start": "npx nodemon -x 'npx yalc push' & npx tsc --allowSyntheticDefaultImports --outDir dist -m ESNext -t es5 --lib es6,dom,dom.iterable,esnext --sourcemap --jsx react --moduleResolution Node --skipLibCheck --resolveJsonModule index.ts"
  }
}
